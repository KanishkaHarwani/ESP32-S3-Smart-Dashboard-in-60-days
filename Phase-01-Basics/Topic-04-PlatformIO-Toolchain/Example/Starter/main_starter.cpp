// Topic 04 — PlatformIO Toolchain
// Starter: blink an LED on real hardware (Xiao ESP32S3).
// Copy this file into your PlatformIO project's src/ folder as main.cpp.

#include <Arduino.h>

// TODO: define the pin the LED is connected to (e.g. D0)
const uint8_t LED_PIN = 0; // placeholder — set to your actual pin

void setup() {
  // TODO: set LED_PIN as an OUTPUT
}

void loop() {
  // TODO: turn the LED on, wait 1 second
  // TODO: turn the LED off, wait 1 second
}
